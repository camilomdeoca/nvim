return {
    renderer = {
        highlight_diagnostics = "name",
        highlight_git = "name",
        indent_markers = {
            enable = true,
        },
    },
    diagnostics = {
        enable = true,
        show_on_dirs = true,
    },
}
